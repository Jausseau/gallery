import styled from "styled-components";

const Container = styled.div`
  display: flex;
  flex-direction: column;
  width: 100%;
  height: calc(100% - 54px);
  gap: 1rem;
  align-items: center;
`;

const GalleryStyles = { Container };

export default GalleryStyles;
