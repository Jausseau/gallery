import styled from "styled-components";

const ListContainer = styled.div`
  align-items: center;
  display: flex;
  flex-wrap: wrap;
  gap: 1rem;
  justify-content: center;
  height: 100%;
  overflow: auto;
  text-align: center;
`;

const ListStyles = { ListContainer };

export default ListStyles;
