import styled from "styled-components";

const Container = styled.div`
  display: flex;
  gap: 0.5rem;
  justify-content: center;
`;

const SearchStyles = { Container };

export default SearchStyles;
